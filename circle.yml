machine:
    java:
        version:
            oraclejdk8
general:
    artifacts:
        - mvn -Denvironment=staging package
deployment:
    staging:
        branch: master
        heroku:
            appname: app-store-app
